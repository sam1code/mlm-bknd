dev:
	@echo "Starting backend-express development server"
	npm run dev

set-up:
	@echo "Setting up backend-express"
	npm install

start:
	@echo "Starting backend-express server"
	npm start

clean:
	@echo "Cleaning up backend-express"
	rm -rf node_modules